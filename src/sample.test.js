test('test1', () => {
  expect(2).toBe(2);
});

// backend game of chess in two hours
// what is in n out of scope of work
// single player or multiplayer (polling or event driven)
// persist or keep in memory
// selection of technologies